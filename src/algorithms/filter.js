// This function will return an array of strings.  Each string should
// match the firstLetter given.  If isCaseSensitive is true, then
// it should only match lower case or upper case.  If isCaseSensitive
// is false, it should match both
//
// Examples:
// filterByFirstLetter(["apple", "berry", "Avocado"], 'a', true);
//   should return: ["apple"]
//
// filterByFirstLetter(["apple", "berry", "Avocado"], 'b', true);
//   should return: ["berry"]
export const filterByFirstLetter = (
    array,
    firstLetter,
    isCaseSensitive = true,
) => {
    return null;
};
